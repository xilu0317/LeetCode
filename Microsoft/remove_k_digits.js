/**
 * @param {string} num
 * @param {number} k
 * @return {string}
 */
const removeKdigits = (num, k) => {
    // num += '';

    // let stack = [];
    // let res = '';
    // for (let i = 0; i < num.length; i++) {
    //     let cur = num[i];

    //     stack.push(cur);

    //     if (stack[stack.length - 1] > cur && k > 0) {
    //         stack.pop();
    //         k--;
    //     }

    //     res += cur;
    // }

    // return res;
};